1. enter "chmod 644 *.php" so that all .php files are readable to everyone writable to owner.
2. enter "chmod 666 *.txt" so that the events.txt is readable and writable to everyone(input.php need access to modify it)
3. enter "chmod 666 models" and repeat step 1 for files inside models folder
4. enter "chmod 666 views" and repeat step 1 for files inside views folder

You can login with login ID: Smitty and Password: JS123
then do what ever you want.

input.php will create a .txt file following the users name so that different users can create their own calendars.
logout button will terminate the session and log out.

